import { Card } from '@material-ui/core';
import React, { useState } from 'react';

function FavoriteCard({favourite}) {
    
    return (
        <div>
            <Card>
                
            </Card>
        </div>
    );
}

export default FavoriteCard;